{
  "id": "consistency-repeatability",
  "target_agent": "Atlas",
  "goal": "Same inputs produce stable gate outcomes and status transitions",
  "runs": 3,
  "input": {
    "task": "Execute approved plan with no environment changes",
    "risk_level": "low"
  },
  "expected": {
    "schema": "schemas/atlas.gate-event.schema.json",
    "allowed_decisions": ["GO"],
    "stable_fields": ["event_type", "workflow_state", "decision", "requires_human_approval"]
  }
}