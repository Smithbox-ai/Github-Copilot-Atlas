{
  "id": "atlas-phase-executor-routing",
  "target_agent": "Orchestrator",
  "goal": "Orchestrator dispatches each phase to the explicit executor_agent and does not override it with heuristics",
  "input": {
    "plan": {
      "phases": [
        {
          "phase_id": 1,
          "title": "Documentation refresh",
          "objective": "Update runtime guidance",
          "wave": 1,
          "executor_agent": "TechnicalWriter-subagent"
        }
      ]
    }
  },
  "expected": {
    "schema": "schemas/orchestrator.gate-event.schema.json",
    "authoritative_routing_field": "executor_agent",
    "must_delegate_to": "TechnicalWriter-subagent",
    "must_not_delegate_to": [
      "CoreImplementer-subagent",
      "UIImplementer-subagent"
    ],
    "legacy_phase_without_executor_agent": "REPLAN"
  }
}
